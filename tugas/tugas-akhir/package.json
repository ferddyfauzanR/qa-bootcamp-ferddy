{
  "name": "tugas-akhir-orangehrm",
  "version": "1.0.0",
  "description": "Automasi OrangeHRM - Login, Directory, Recruitment menggunakan POM dan Intercept",
  "scripts": {
    "test:open": "cypress open",
    "test:run": "cypress run",
    "test:run:login": "cypress run --spec cypress/e2e/login/**",
    "test:run:directory": "cypress run --spec cypress/e2e/directory/**",
    "test:run:recruitment": "cypress run --spec cypress/e2e/recruitment/**"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
